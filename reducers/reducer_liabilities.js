const initialState = {
	list: null,
	map: null,
	isFetching: false
}

const liabilities = (state = initialState, action) => {
	switch(action.type) {
		default:
			return state
	}
}

export default liabilities